import type{Packet,Result,SeqState}from'./types';export function sequence(p:Packet,s:SeqState):Result{s.received++;if(s.last==null){s.last=p.seq;return{status:'TELEMETRY_OK',packet:p}}if(p.seq===s.last)return{status:'TELEMETRY_DUPLICATE_PACKET',field:'seq',value:p.seq};if(p.seq>s.last+1){const lost=p.seq-s.last-1;s.lost+=lost;s.last=p.seq;return{status:'TELEMETRY_SEQUENCE_GAP',packet:p,field:'seq',value:p.seq,expected:'previous + 1',lost}}s.last=p.seq;return{status:'TELEMETRY_OK',packet:p}}