import type{Packet,Result}from'./types';const n=(v:any)=>typeof v==='number'&&Number.isFinite(v);export function validate(p:Packet):Result{const checks:[string,any,number,number][]=[['pos.lat',p.pos.lat,-90,90],['pos.lon',p.pos.lon,-180,180],['pos.alt_km',p.pos.alt_km,0,100000],['pos.vel_kms',p.pos.vel_kms,0,20],['pos.gnss_sats',p.pos.gnss_sats,0,64],['pos.pdop',p.pos.pdop,0,100],['eps.bus_v',p.eps.bus_v,0,20],['eps.bat_soc',p.eps.bat_soc,0,100],['eps.bat_t',p.eps.bat_t,-100,150],['eps.solar_w',p.eps.solar_w,0,1000],['obc.cpu',p.obc.cpu,0,100],['comm.rssi_dbm',p.comm.rssi_dbm,-200,0],['comm.snr_db',p.comm.snr_db,-50,100]];for(const[c,v,a,b]of checks){if(!n(v))return{status:'TELEMETRY_INVALID_TYPE',field:c,value:v,expected:'number'};if(v<a||v>b)return{status:'TELEMETRY_INVALID_RANGE',field:c,value:v,expected:`${a} <= value <= ${b}`}}return{status:'TELEMETRY_OK',packet:p}}